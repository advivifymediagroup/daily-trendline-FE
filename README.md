# Daily Trendline FE

This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Prerequisites

Make sure you have the following installed:

* Node.js (v18 or later recommended)
* pnpm (recommended package manager)

Install pnpm globally if you don't have it:

```bash
npm install -g pnpm
```

## Installation

Clone the repository:

```bash
git clone https://github.com/advivifymediagroup/daily-trendline-FE.git
```

Navigate into the project folder:

```bash
cd daily-trendline-FE
```

Install dependencies using **pnpm**:

```bash
pnpm install
```

## Getting Started

Run the development server:

```bash
pnpm dev
```

Open your browser and visit:

```
http://localhost:3000
```

You can start editing the page by modifying:

```
app/page.tsx
```

The page will automatically update as you edit the file.

## Build for Production

To create a production build:

```bash
pnpm build
```

To start the production server:

```bash
pnpm start
```

## Linting

Run the linter:

```bash
pnpm lint
```

## Learn More

To learn more about Next.js, take a look at the following resources:

* https://nextjs.org/docs – Next.js documentation
* https://nextjs.org/learn – Interactive Next.js tutorial

## Deployment

The easiest way to deploy your Next.js app is via **Vercel**:

https://vercel.com/new

For more details, check the official deployment documentation:

https://nextjs.org/docs/app/building-your-application/deploying
